[package]
name = "bdk-bip322"
version = "0.1.0"
edition = "2024"

[dependencies]
bitcoin = { version = "0.32.5", features = [ "base64" ], default-features = false }

[features]
default = ["std"]
std = []